// The 104-card quiz bank. The original 56 cards were ported from
// reference/onsite-express.html, then hand-rebalanced so option length gives
// nothing away; q57+ are new. This file is authored by hand now — do NOT
// regenerate it with scripts/extract-data.mjs, that would resurrect the old bank.

import type { Deck, QType, Question } from "../types";

/* Quiz bank. t: pat|bug|line|cx · ph: graphs|bt|dp|ds · a is index into o BEFORE
   shuffle — the correct option is always stored first and never displayed first. */
export const Q: Question[] = [
{ id:"q1", t:"pat", ph:"graphs", q:"A grid of 0s and 1s. Count the groups of connected 1s.",
  o:["Flood fill from each unvisited land cell","Row-by-row sliding window over runs of 1s","Topological sort of cells by neighbour count","Binary search on the count of islands"], a:0,
  x:"Connected components on a grid: scan, flood every unseen island, count the floods. Row runs are the trap — one island spans many rows, and two runs in the same row can already be joined through the row below." },
{ id:"q2", t:"pat", ph:"graphs", q:"Accounts must be merged whenever they share an email address.",
  o:["Union-Find with emails as the nodes","A trie keyed on shared email prefixes","Sort every email; merge adjacent duplicates","Dijkstra over the account–email link graph"], a:0,
  x:"Union everything inside a row, then group by root — Accounts Merge. Sorting spots pairs that share an email but not chains: a↔b plus b↔c must collapse into one account, and only a root structure carries that transitively." },
{ id:"q3", t:"pat", ph:"graphs", q:"Order the courses so every prerequisite comes first.",
  o:["Topological sort — Kahn's or DFS post-order","BFS from course 0 in plain discovery order","Union-Find, merging courses with their prereqs","Greedy by total degree, smallest course first"], a:0,
  x:"Dependencies form a DAG; topo order is its sorted spine, cycle ⇒ impossible — Course Schedule II. BFS from course 0 orders by distance from one node and never reaches prereq-free courses elsewhere; Union-Find forgets direction entirely." },
{ id:"q4", t:"pat", ph:"graphs", q:"Cheapest route from a to b using at most k hops.",
  o:["Bellman-Ford, k+1 rounds off a frozen snapshot","Heap Dijkstra as-is, stopping after k pops","0-1 BFS with each hop as a zero-one weight","DFS memoised on the current node alone"], a:0,
  x:"Dijkstra commits greedily and can't respect a hop budget — the cheap path may spend too many edges, your exact Cheapest Flights wall; Bellman-Ford rounds count hops for free. Memoising on node alone conflates states with different hops left." },
{ id:"q5", t:"pat", ph:"graphs", q:"Reach the corner while minimising the single largest step you ever take.",
  o:["Dijkstra relaxing with max(effort, step), not +","Plain BFS carrying the largest step so far","Greedy walk to the nearest-height neighbour","Backtracking over every path to the corner"], a:0,
  x:"Minimax relaxation: same heap loop, max() instead of + — Path With Minimum Effort. BFS is the trap: it optimises hop count, and a short path can hide one giant step that a longer path avoids." },
{ id:"q6", t:"pat", ph:"graphs", q:"Shortest path where every edge costs exactly 0 or 1.",
  o:["0-1 BFS — free edges appendleft on a deque","Plain BFS with both weights read as one hop","Bellman-Ford sweeping all edges V−1 times","DFS re-expanding nodes on any cheaper path"], a:0,
  x:"Free edges cut the queue line, so the deque holds at most two costs in sorted order — O(V+E). Ignoring weights is fatal: a chain of three free edges is closer than one paid edge, and hop-count BFS ranks it farther." },
{ id:"q17", t:"pat", ph:"graphs", q:"Fewest one-letter mutations from one word to another through a dictionary.",
  o:["BFS over the implicit one-letter-apart graph","DFS with memoisation keyed on the current word","Dijkstra with every single mutation weighted at 1","Backtracking with pruning on the best path length"], a:0,
  x:"Unweighted shortest path ⇒ BFS — Word Ladder. Memoised DFS is the tempting one: the word graph has cycles, so cached depths depend on the path that filled them and come out wrong. Dijkstra merely re-derives BFS with heap overhead." },
{ id:"q18", t:"pat", ph:"graphs", q:"Does this directed graph contain a cycle?",
  o:["Three-colour DFS — grey hit again means cycle","Union-Find — flag any edge landing inside one set","One visited set, flagging any revisited node","An in-degree-0 scan — none left means a cycle"], a:0,
  x:"Grey means on the current stack; only grey→grey closes a directed loop — black revisits are diamonds, not cycles. Union-Find is blind to direction: a→b, c→b, a→c merges one set with no cycle anywhere in sight." },
{ id:"q22", t:"bug", ph:"graphs", q:"This BFS is correct but explodes on big graphs. The flaw?",
  code:"while q:\n    u = q.popleft()\n    seen.add(u)\n    for v in adj[u]:\n        if v not in seen:\n            q.append(v)",
  o:["Marks on pop — move seen.add to enqueue time","popleft on a deque is O(n) — pop the right end","seen should map each node to its distance","adj[u] is unsorted, so the visit order drifts"], a:0,
  x:"Between enqueue and pop a node is invisible to the membership check, so duplicates flood the queue — exponential on dense graphs. deque.popleft is already O(1); that horror story belongs to list.pop(0)." },
{ id:"q23", t:"bug", ph:"graphs", q:"A heap Dijkstra that mysteriously does far too much work.",
  code:"while h:\n    d, u = heappop(h)\n    for v, w in adj[u]:\n        if d + w < dist[v]:\n            dist[v] = d + w\n            heappush(h, (dist[v], v))",
  o:["Stale pops aren't skipped — d > dist[u] ⇒ continue","The push tuple is backwards — node must go first","heapq lacks decrease-key — re-heapify each round","The relaxation must read dist[u] + w, not d + w"], a:0,
  x:"Lazy deletion: superseded heap entries must be skipped on pop or their whole neighbourhood re-expands — your Network Delay Time guard. No rebuilds needed; duplicates are harmless once stale ones get the continue." },
{ id:"q24", t:"bug", ph:"graphs", q:"Kahn's algorithm that 'works' on every input. What's missing?",
  code:"order = []\nwhile q:\n    u = q.popleft(); order.append(u)\n    for v in adj[u]:\n        indeg[v] -= 1\n        if indeg[v] == 0: q.append(v)\nreturn order",
  o:["len(order) == n is never checked at the end","indeg[v] can go negative, re-queueing a node twice","The deque must be a stack for a valid order","order arrives reversed — flip it on return"], a:0,
  x:"Nodes on a cycle never reach in-degree 0, so they quietly never appear — short output is the only symptom; compare len(order) to n. Reversing is DFS post-order's ritual, not Kahn's; stack vs queue only changes which valid order you get." },
{ id:"q30", t:"bug", ph:"graphs", q:"Bellman-Ford capped at k stops, but answers use too many edges.",
  code:"for _ in range(k + 1):\n    for u, v, w in edges:\n        if dist[u] + w < dist[v]:\n            dist[v] = dist[u] + w",
  o:["Rounds share one array — relax off a frozen copy","range(k + 1) runs one round too many — use range(k)","The edge list must be weight-sorted before every sweep","The < ought to be <=, so equal-cost ties propagate"], a:0,
  x:"A shared array lets one round chain several new edges — the Cheapest Flights leak; read last round's snapshot, write this round's. k stops means k+1 edges, so k+1 rounds is right, and edge order is irrelevant once reads are frozen." },
{ id:"q35", t:"bug", ph:"graphs", q:"A DSU whose components silently merge wrong.",
  code:"def union(a, b):\n    parent[a] = b",
  o:["It links raw nodes — find() both roots first","The assignment is flipped — set parent[b] = a","No rank tracking, so the trees grow too tall","Children of a must be re-pointed recursively"], a:0,
  x:"Re-pointing a mid-chain node splices its subtree off its old set — union roots, never raw nodes. Rank is a speed upgrade, not a correctness fix, and children need no touching: find() resolves through whatever their ancestors point at." },
{ id:"q38", t:"line", ph:"graphs", q:"Fill the BFS blank.",
  code:"if v not in seen:\n    ____\n    q.append(v)",
  o:["seen.add(v)","seen.add(u)","q.appendleft(v)","seen.discard(v)"], a:0,
  x:"Mark on enqueue — v must be visible to the membership check the instant it joins the queue, or duplicates pour in. Adding u is a no-op: it was marked at its own enqueue. appendleft is 0-1 BFS's move, not this one." },
{ id:"q41", t:"line", ph:"graphs", q:"DSU find with path halving.",
  code:"while parent[x] != x:\n    ____\n    x = parent[x]",
  o:["parent[x] = parent[parent[x]]","x = parent[parent[parent[x]]]","parent[parent[x]] = parent[x]","parent[parent[x]] = x"], a:0,
  x:"Hop your parent pointer up to the grandparent as you walk — trees stay near-flat with one line and no recursion. Triple-jumping x skips the write that does the flattening; the other two aim the arrow downward and bend the tree into a loop." },
{ id:"q43", t:"line", ph:"graphs", q:"0-1 BFS, on a zero-cost edge.",
  code:"if w == 0:\n    ____\nelse:\n    q.append((v, d + 1))",
  o:["q.appendleft((v, d))","q.append((v, d))","heappush(q, (d, v))","q.appendleft((v, d + 1))"], a:0,
  x:"Cost-d work goes to the front, so the deque never holds more than two costs in order — the poor man's heap. Plain append keeps the right cost in the wrong seat and the O(V+E) bound dies; d + 1 charges for a free edge." },
{ id:"q44", t:"line", ph:"graphs", q:"Dijkstra, right after improving dist[v].",
  code:"dist[v] = nd\n____",
  o:["heappush(h, (nd, v))","heappush(h, (v, nd))","h.append((nd, v))","heappush(h, (dist[u], v))"], a:0,
  x:"Distance rides in slot 0 — the heap's sort key. (v, nd) orders pops by node id and silently breaks cheapest-first; a bare append skips the sift and corrupts the heap; dist[u] files v under its parent's price." },
{ id:"q48", t:"cx", ph:"graphs", q:"Dijkstra with a binary heap?",
  o:["O(E log V) — every push pays a log","O(E + V log V) — the decrease-key bound","O(V²) — scan for the nearest unvisited","O(E · V) — relax all edges, V rounds"], a:0,
  x:"Each edge may push once, each push pays a log: O(E log V). O(E + V log V) is Fibonacci-heap Dijkstra — binary heaps have no O(1) decrease-key. O(V²) is the heapless array scan: fine on dense graphs, not this one." },
{ id:"q57", t:"pat", ph:"graphs", q:"Can the nodes be split into two camps so no edge stays inside one camp?",
  o:["Two-colour BFS, flipping the colour per edge","Union-Find, merging the two ends of each edge","DFS listing all cycles, rejecting odd lengths","Sorting nodes into camps by degree parity"], a:0,
  x:"Colour a node, push the opposite colour across every edge; a clash is an odd cycle — Is Graph Bipartite. Merging endpoints does the reverse: it declares the two ends teammates, the one thing an edge forbids (union u with v's enemy is the DSU repair)." },
{ id:"q58", t:"pat", ph:"graphs", q:"Rot spreads to neighbouring oranges each minute. Minutes until nothing fresh remains?",
  o:["Multi-source BFS, all rotten cells seeded at 0","BFS from each rotten cell, keeping cell minima","DFS from each rotten cell, minute as the depth","Dijkstra with every infection step weighted 1"], a:0,
  x:"One queue, every source at minute 0 — the rings advance together and each fresh cell rots at its true earliest minute. Rotting Oranges, Walls and Gates. Per-source BFS re-sweeps the grid k times for the same minima; DFS depth is path length, not time." },
{ id:"q59", t:"pat", ph:"graphs", q:"Does this undirected graph contain a cycle?",
  o:["Union-Find — an edge inside one set closes a loop","Three-colour DFS, grey meeting grey on the stack","One shared visited set; any revisit is a cycle","Kahn's peeling by in-degree until none remains"], a:0,
  x:"No directions to respect, so DSU is finally legal: union each edge, and an edge whose ends already share a root closes a loop. The visited-set trap: DFS immediately re-sees the node it came from — skip the parent or every edge cries cycle." },
{ id:"q60", t:"pat", ph:"graphs", q:"Wire all n offices into one network with the least total cable.",
  o:["Kruskal — sort cables, union what doesn't cycle","Dijkstra from HQ, then sum the shortest paths","Greedy: each office takes its cheapest cable out","BFS from HQ, keeping the first cable that reaches"], a:0,
  x:"Minimum spanning tree — Min Cost to Connect All Points: sort edges, DSU-skip anything that closes a cycle (or Prim outward with a heap). Dijkstra optimises each route from HQ separately; a union of shortest paths can cost far more wire than the MST." },
{ id:"q61", t:"bug", ph:"graphs", q:"Clone Graph by bare recursion — RecursionError on the first sample. Why?",
  code:"def clone(u):\n    copy = Node(u.val)\n    for v in u.nbrs:\n        copy.nbrs.append(clone(v))\n    return copy",
  o:["No old→new dict, so any cycle recurses forever","A visited set is missing — skip repeat neighbours","copy.nbrs aliases u.nbrs — slice it defensively","Node(u.val) shares val — deep-copy the payload"], a:0,
  x:"Two nodes pointing at each other ping-pong the recursion forever. A dict old→new, checked before constructing, breaks the loop and hands every repeat visit the same copy. A visited-set skip also halts — but silently drops edges back to nodes already cloned. vals are immutable; sharing is fine." },
{ id:"q62", t:"bug", ph:"graphs", q:"This undirected cycle check claims a single edge is already a cycle.",
  code:"def dfs(u, par):\n    seen.add(u)\n    for v in adj[u]:\n        if v in seen:\n            return True\n        if dfs(v, u):\n            return True\n    return False",
  o:["v == par is never skipped, so every edge trips it","seen.add(u) fires too soon — add after the loop","seen persists across components, hiding cycles","The check needs grey/black states, not one set"], a:0,
  x:"adj[v] contains u — the road you came in on — so the seen test fires on the first backtrack glance. One line fixes it: if v == par: continue. After that one set is plenty: any other seen neighbour genuinely closes a loop; grey/black earns its keep only on directed graphs." },
{ id:"q63", t:"bug", ph:"graphs", q:"A maze BFS whose step counts come out far too large.",
  code:"steps = 0\nwhile q:\n    r, c = q.popleft()\n    if (r, c) == goal: return steps\n    steps += 1\n    for dr, dc in DIRS:\n        nr, nc = r + dr, c + dc\n        if ok(nr, nc) and (nr, nc) not in seen:\n            seen.add((nr, nc)); q.append((nr, nc))",
  o:["steps ticks per pop — advance it once per ring","The goal test belongs at enqueue, not at pop","steps += 1 must come before the goal check","seen fills at enqueue — mark at the pop instead"], a:0,
  x:"steps counts cells visited, not rings crossed: freeze a layer with for _ in range(len(q)), or carry (r, c, d) in the queue. Goal-at-enqueue is a fair optimisation, not the bug — and mark-on-enqueue is the half of this code that's already right; don't fix it backwards." },
{ id:"q64", t:"line", ph:"graphs", q:"Topological sort by DFS — fill the post-order line.",
  code:"def dfs(u):\n    state[u] = GREY\n    for v in adj[u]:\n        if state[v] == WHITE: dfs(v)\n    state[u] = BLACK\n    ____\n# afterwards: order.reverse()",
  o:["order.append(u)","order.extend(adj[u])","order.insert(0, u)","order.appendleft(u)"], a:0,
  x:"Append u once every descendant is finished; the final reverse turns post-order into topo order. insert(0)/appendleft pre-reverse, so the closing reverse would undo them — and appendleft is a deque verb lists lack. extend dumps children a second time." },
{ id:"q65", t:"line", ph:"graphs", q:"Max Area of Island — the line that sinks the cell.",
  code:"def area(r, c):\n    if not ok(r, c) or grid[r][c] == 0:\n        return 0\n    ____\n    return 1 + sum(area(r + dr, c + dc) for dr, dc in DIRS)",
  o:["grid[r][c] = 0","grid[r][c] = -1","seen.add((r, c))","return grid[r][c]"], a:0,
  x:"Sink to 0 — the guard's only stop sign. Marking with −1 sails straight past == 0 and recurses forever; a seen set the guard never reads is decoration; an early return counts one cell and strands the rest of the island." },
{ id:"q66", t:"line", ph:"graphs", q:"Word Ladder — build the one-letter mutation of w at position i.",
  code:"for i in range(len(w)):\n    for c in \"abcdefghijklmnopqrstuvwxyz\":\n        ____\n        if nxt in words and nxt not in seen:\n            seen.add(nxt); q.append(nxt)",
  o:["nxt = w[:i] + c + w[i+1:]","nxt = w[:i] + c + w[i:]","nxt = w.replace(w[i], c, 1)","nxt = w.replace(w[i], c)"], a:0,
  x:"Slice position i out exactly. replace targets a letter, not a position — with count 1 it swaps the first match wherever it sits (\"pop\", i = 2 edits index 0), unbounded it swaps them all. And w[i:] keeps the old letter: the word grows." },
{ id:"q67", t:"cx", ph:"graphs", q:"BFS across an m × n grid, four moves per cell?",
  o:["O(m·n) — every cell enqueues at most once","O(m·n log(m·n)) — heap-ordered pops","O(m²·n²) — cell pairs, worst case","O(4^(m+n)) — four choices, every step"], a:0,
  x:"Each cell enqueues once behind the seen check and contributes four edges: linear in cells. The log rider belongs to Dijkstra's heap — unit steps don't need it; 4^(m+n) is the unguarded all-paths count, exactly what the visited set deletes." },
{ id:"q68", t:"cx", ph:"graphs", q:"n mixed union/find ops, union by rank plus path compression — tight amortised cost per op?",
  o:["O(α(n)) — inverse Ackermann, near-flat","O(log n) — union by rank's bound","O(log* n) — the iterated-logarithm bound","O(1) — trees flatten to depth one"], a:0,
  x:"Inverse Ackermann: at most 4 for any n that fits in the universe, yet provably not O(1) — compression never quite finishes flattening. log n is rank without compression; log* n was the bound before Tarjan tightened it. All real, none tight." },

{ id:"q7", t:"pat", ph:"bt", q:"Generate every subset of a list of distinct numbers.",
  o:["Include/exclude DFS moving a start index only forward","Permutation-style backtracking around a shared used set","A 2D DP over (index, size) tallying subsets by size","Sort, then slide a window across every contiguous stretch"], a:0,
  x:"Include/exclude with a start index gives each subset exactly one spelling. A used set is the permutation move — it would emit each subset once per ordering. Windows only see contiguous runs; subsets skip." },
{ id:"q8", t:"pat", ph:"bt", q:"Generate every ordering of distinct items.",
  o:["Backtracking on a used set, or in-place prefix swaps","Backtracking on a start index that never looks back","Kahn's algorithm over a precedence graph of the items","Sort, then emit every rotation with two chasing pointers"], a:0,
  x:"Order matters, so earlier items must stay pickable — a used set (or swapping the prefix) does that. A start index forbids going back: that's combinations. Rotations reach n of the n! orderings." },
{ id:"q16", t:"pat", ph:"bt", q:"Find which of 10,000 words appear in a letter grid.",
  o:["Build one trie of all the words, backtrack once from every cell","Run the single-word Word Search once for each of the words","BFS from every cell, checking frontiers against a word set","Hash every simple path in the grid, then intersect the sets"], a:0,
  x:"One traversal serves every word — step only into letters that are live trie children, prune the instant none match. 10,000 separate Word Search runs re-walk the same grid 10,000 times." },
{ id:"q21", t:"bug", ph:"bt", q:"Subsets via include/exclude — but the output is garbage. Why?",
  code:"def dfs(i):\n    if i == len(nums):\n        res.append(path[:]); return\n    dfs(i + 1)              # exclude\n    path.append(nums[i])    # include\n    dfs(i + 1)",
  o:["path is never popped after the include branch","res should collect path itself, not a path[:] copy","The exclude call has to come after the include call","The base case fires early — it needs i > len(nums)"], a:0,
  x:"Choose without un-choose: after the include's dfs returns, pop — or nums[i] leaks into every sibling branch. The copy is required (path keeps mutating), and branch order only changes output order." },
{ id:"q29", t:"bug", ph:"bt", q:"Word Search finds some words but misses others that clearly exist.",
  code:"def dfs(r, c, i):\n    if i == len(w): return True\n    if bad(r, c) or (r, c) in seen: return False\n    if grid[r][c] != w[i]: return False\n    seen.add((r, c))\n    return any(dfs(r+dr, c+dc, i+1)\n               for dr, dc in DIRS)",
  o:["seen never releases (r, c) — discard it after exploring","seen.add fires too early — it belongs after the any() call","any() short-circuits — all() is required across directions","The i == len(w) base case stops one letter shy"], a:0,
  x:"The seen set is borrowed, not kept: discard (r, c) once all four directions fail, or one failed path poisons every later one through that cell. Marking after the recursion would let a path revisit its own cells; all() would demand every direction succeed." },
{ id:"q31", t:"bug", ph:"bt", q:"An N-Queens attempt rejects a square only when its column is taken. Missing?",
  o:["Diagonals — track the r−c and r+c sets too","Row attacks — a row set has to be maintained as well","Nothing — one queen per row makes columns sufficient","Knight-move conflicts between queens two rows apart"], a:0,
  x:"Row-by-row placement makes rows free and columns are checked — but queens still meet along both diagonals: constant r−c one way, constant r+c the other. Knights are a different piece." },
{ id:"q32", t:"bug", ph:"bt", q:"Combination Sum II on a sorted array still emits duplicate combinations.",
  code:"for i in range(start, len(nums)):\n    path.append(nums[i])\n    bt(i + 1)\n    path.pop()",
  o:["Add: if i > start and nums[i] == nums[i-1]: continue","Recurse with bt(i) instead of bt(i + 1) to allow reuse","Skip the sort — sorting is what lines the dupes up","Move path.pop() before the bt(i + 1) call, not after"], a:0,
  x:"Two equal values tried at the same tree level spell the same set twice — skip the repeat at the level, keep it in the branch. bt(i) is Combination Sum I's reuse move; here each element is used once. The sort is what makes the adjacent-skip work." },
{ id:"q36", t:"line", ph:"bt", q:"Complete the skeleton.",
  code:"path.append(nums[i])\nbt(i + 1, path)\n____",
  o:["path.pop()","path.clear()","res.append(path)","return path"], a:0,
  x:"The un-choose — pop exactly what you pushed, or nums[i] leaks into the next branch. clear() nukes the ancestors' picks too; appending here would record the path after the recursion already handled it." },
{ id:"q46", t:"cx", ph:"bt", q:"How many subsets does an n-element set have?",
  o:["2^n","n!","n²","n·2^(n−1)"], a:0,
  x:"In or out: two choices per element, multiplied. n! counts orderings, and n·2^(n−1) is the total size of all subsets combined — not how many there are." },
{ id:"q47", t:"cx", ph:"bt", q:"Time to build all permutations of n distinct items?",
  o:["O(n · n!)","O(n!)","O(n^n)","O(2^n · n)"], a:0,
  x:"n! outputs, each of length n — the copy at the leaf costs too. Bare O(n!) forgets that copy; n^n ignores that the used set prunes repeated picks." },
{ id:"q56", t:"pat", ph:"bt", q:"Return every root-to-leaf path itself — not just the count.",
  o:["DFS a shared path list: copy at leaves, pop on the way out","Level-order with a queue holding (node, depth) pairs","Morris traversal, threading each path through null links","BFS tallying the leaves as the frontier reaches them"], a:0,
  x:"The path is a live object the recursion edits and repairs — snapshot it at each leaf. (node, depth) pairs forget ancestry, Morris trades away the very stack that holds your path, and a tally is exactly the count we weren't asked for." },

{ id:"q69", t:"pat", ph:"bt", q:"Combination Sum: candidates may be reused — return every combination hitting target.",
  o:["Start-index backtracking that recurses with i, not i + 1","Start-index backtracking that recurses with i + 1 each pick","Unbounded-knapsack DP over every amount up to the target","Sort, then squeeze two pointers inward toward the target"], a:0,
  x:"Reuse means staying on i after picking it — the branch may take the same candidate again. i + 1 is Combination Sum II's no-reuse move. The knapsack DP counts or optimises; it never hands back the combinations themselves." },
{ id:"q70", t:"pat", ph:"bt", q:"Ways-to-make-target: when is backtracking the right tool over DP?",
  o:["When the answer is the objects, not how many exist","When candidates can repeat — DP cannot model reuse","When the target is huge and a DP table won't fit in memory","When the input is unsorted — DP needs its stages ordered"], a:0,
  x:"Counts let states merge — one DP cell folds exponentially many builds into a number. Needing each build forces the exponential walk: backtracking. Reuse is modelled fine by unbounded knapsack, and neither tool cares how the input arrives." },
{ id:"q71", t:"pat", ph:"bt", q:"Every letter string a phone number can spell (Letter Combinations, LC 17).",
  o:["DFS one digit per depth, one branch per key letter","Permutation backtracking with a used set over the letters","Memoised DP on digit suffixes to reuse repeated expansions","Enumerate base-3 strings, mapping each digit onto a letter"], a:0,
  x:"Depth = position in the number, branches = that key's 3-4 letters; every depth-n path is an answer. A used set would permute letters across keys into strings the phone can't spell, no suffix repeats inside one number so the memo never hits, and keys 7 and 9 carry four letters — base-3 breaks." },
{ id:"q72", t:"pat", ph:"bt", q:"Split a string so every piece is a palindrome — return all such splits (LC 131).",
  o:["Backtrack on cut points, recursing only past palindrome prefixes","Interval DP that returns the minimum number of cuts required","Expand around every center, then greedily merge the intervals","Manacher's algorithm, then one sweep to stitch pieces together"], a:0,
  x:"Choose a palindromic prefix, recurse on the remainder, pop on return — refusing non-palindrome prefixes is the pruning. Min-cut DP is Partitioning II: one number out, not every split. Centers and Manacher find palindromes; they don't enumerate partitions." },
{ id:"q73", t:"bug", ph:"bt", q:"Generate Parentheses emitting strings like \")(\" — find the leak.",
  code:"def bt(s, op, cl):\n    if len(s) == 2 * n:\n        res.append(s); return\n    if op < n:\n        bt(s + \"(\", op + 1, cl)\n    if cl < n:\n        bt(s + \")\", op, cl + 1)",
  o:["The close guard is wrong — it must be cl < op, not cl < n","Strings skip the un-choose — each branch needs a pop","The base case must additionally verify that op == cl","res.append(s) captures a reference that later branches mutate"], a:0,
  x:"cl < n gladly closes brackets never opened — cl < op is the entire pruning. Checking balance at the end can't rescue it: \")(\" is balanced yet invalid. And s is immutable — every call gets its own copy, so there is nothing to pop and nothing to alias." },
{ id:"q74", t:"bug", ph:"bt", q:"Subsets with duplicates, sorted and skipping — yet [2,2] never shows up.",
  code:"nums.sort()\ndef bt(start):\n    res.append(path[:])\n    for i in range(start, len(nums)):\n        if i > 0 and nums[i] == nums[i-1]:\n            continue\n        path.append(nums[i])\n        bt(i + 1)\n        path.pop()",
  o:["The skip needs i > start — i > 0 also blocks in-branch picks","res.append belongs in a base case, not at the top of every call","bt(i + 1) forbids reuse — duplicates require bt(i)","path[:] snapshots too soon — the copy belongs after the loop"], a:0,
  x:"i > start skips a repeated value only when it re-appears at the same level, as a sibling. i > 0 also vetoes it one level deeper, so the second 2 can never follow the first. Appending at every node IS the subsets collection; bt(i) would let one element appear twice." },
{ id:"q75", t:"bug", ph:"bt", q:"This Sudoku solver reaches a full solution, yet the board comes back untouched.",
  code:"def solve():\n    cell = next_empty()\n    if not cell: return True\n    r, c = cell\n    for d in \"123456789\":\n        if ok(r, c, d):\n            board[r][c] = d\n            solve()\n            board[r][c] = \".\"\n    return False",
  o:["solve()'s result is dropped — success must propagate up","The board[r][c] = \".\" reset erases good digits — delete it","next_empty() must be re-run after each placement, in the loop","The loop must first drop digits that row r already contains"], a:0,
  x:"Even after a branch solves the board, control falls through to the reset and scrubs it — catch it with if solve(): return True. The \".\" undo is essential on failure; deleting it leaves dead digits behind. Recursion already re-finds the next empty cell." },
{ id:"q76", t:"line", ph:"bt", q:"N-Queens, placed row by row. Complete the safety test.",
  code:"def safe(r, c):\n    return ____",
  o:["c not in cols and r-c not in dg1 and r+c not in dg2","c not in cols and r not in rows and (r, c) not in seen","c not in cols and abs(r - c) not in the diag set","all(qc != c and r - qr != c - qc for qr, qc in queens)"], a:0,
  x:"One set per attack line: columns, ↘ diagonals keyed by r−c, ↙ keyed by r+c. Rows come free when each row hosts exactly one queen, and r − qr != c − qc alone checks a single diagonal direction — the mirrored one slips through." },
{ id:"q77", t:"line", ph:"bt", q:"Palindrome Partitioning — recurse only past a qualifying piece.",
  code:"for end in range(i + 1, len(s) + 1):\n    piece = s[i:end]\n    if ____:\n        path.append(piece)\n        bt(end)\n        path.pop()",
  o:["piece == piece[::-1]","s[i] == s[end - 1]","piece not in path","len(piece) % 2 == 1"], a:0,
  x:"A piece earns its cut only if it reads the same reversed. Matching end letters isn't enough — \"abca\" passes that test — and palindromes come even-length too (\"abba\"). Repeats across the path are perfectly legal." },
{ id:"q78", t:"line", ph:"bt", q:"Restore IP Addresses — which segments deserve the recursion?",
  code:"for j in range(i + 1, min(i + 4, len(s) + 1)):\n    seg = s[i:j]\n    if ____:\n        bt(j, parts + [seg])",
  o:["int(seg) <= 255 and (seg == \"0\" or seg[0] != \"0\")","int(seg) < 255 and (seg == \"0\" or seg[0] != \"0\")","seg == \"0\" or (seg[0] != \"0\" and len(seg) <= 3)","int(seg) <= 255 and not seg.startswith(\"00\")"], a:0,
  x:"Two rules: value ≤ 255, and no leading zero unless the segment is exactly \"0\". The strict < orphans 255 itself; dropping the cap admits 999; and banning only \"00…\" still waves \"01\" through." },
{ id:"q79", t:"cx", ph:"bt", q:"Generate Parentheses with op/cl guards — total work for n pairs?",
  o:["O(4^n / √n) — Catalan-many valid strings","O(2^(2n)) — every bracket string of length 2n","O(n · n!) — orderings of the n bracket pairs","O(n · 2^n) — an include/exclude tree over pairs"], a:0,
  x:"The guards admit exactly the Catalan count, ~4^n/n^1.5 strings, each copied at O(n) — O(4^n/√n) total. 2^(2n) prices the unpruned universe; the counters exist precisely so you never pay it." },
{ id:"q80", t:"cx", ph:"bt", q:"Palindrome Partitioning, worst case — a length-n string of one repeated letter?",
  o:["O(n · 2^n) — 2^(n−1) partitions, each copied out","O(n³) — n² substrings, each checked in O(n)","O(2^n) — enumeration only, the copies amortise away","O(n² · 2^n) — a fresh palindrome scan at every node"], a:0,
  x:"Every cut point is free, so 2^(n−1) partitions exist, and emitting each costs O(n) — O(n·2^n). The n³ figure only prices the palindrome table, forgetting the exponential enumeration it feeds; the copies never amortise away." },

{ id:"q9", t:"pat", ph:"dp", q:"Can the array be split into two piles with equal sums?",
  o:["0/1 knapsack: can any subset hit total/2","Greedy: sort desc, keep feeding the lighter pile","Prefix sums + binary search on the split point","Two heaps, balanced to within one element"], a:0,
  x:"Partition Equal Subset Sum. Greedy dies on {8,7,6,5,4}: 8+7 = 6+5+4 = 15, yet its piles land 17/13. Prefix sums only cut the array contiguously — subsets aren't slices. Feasibility DP checks every reachable sum." },
{ id:"q10", t:"pat", ph:"dp", q:"Fewest coins to reach an amount, unlimited supply of each coin.",
  o:["1D DP over amounts — an unbounded knapsack","Greedy: biggest coin that still fits, repeat","Binary search on the answer's coin count","Sort coins, then two pointers inward"], a:0,
  x:"Greedy dies on coins {1,3,4}, amount 6 — 3+3 beats 4+1+1. dp[a] = min(dp[a−c]) + 1 tries every coin at every amount; 'k coins feasible?' has no cheap check, so there's nothing to binary-search." },
{ id:"q11", t:"pat", ph:"dp", q:"Length of the longest strictly increasing subsequence — not necessarily contiguous.",
  o:["dp[i] = 1 + best dp[j] over smaller-valued j < i","A sliding window that resets when the run breaks","Greedy: keep every element beating the last kept one","Kadane on the array of pairwise differences"], a:0,
  x:"Subsequences skip; windows and Kadane demand contiguity. The greedy keeper dies on [5,6,1,2,3,4] — hoarding 5,6 blocks the length-4 tail. O(n²) DP first, patience later." },
{ id:"q12", t:"pat", ph:"dp", q:"Can the string be segmented entirely into dictionary words?",
  o:["DP over prefixes — memoised DFS on the start index","Greedy: peel the longest matching dictionary word each time","Walk a trie of the dictionary once, left to right, no restarts","Compare letter counts of the string against the dictionary's"], a:0,
  x:"Word Break. Greedy longest match dies on \"cars\" + {car, ca, rs} — car strands the s. One trie walk can't back up to a shorter cut either; letter counts ignore order entirely. Try every split point, cache the verdicts." },
{ id:"q13", t:"pat", ph:"dp", q:"Largest all-1 square in a grid — the O(n²) one-pass recurrence.",
  o:["dp[r][c] = 1 + min(top, left, top-left) on each 1-cell","Per-row column histograms + the largest-rectangle stack sweep","Flood fill every blob of 1s and take its bounding square","2D prefix sums with a binary search on side length"], a:0,
  x:"A square grows only as far as its weakest neighbour allows — min of three, plus one. Histograms solve the harder rectangle problem; a blob's bounding square is mostly holes. Prefix sums + binary search works too, but pays an extra log for nothing." },
{ id:"q19", t:"pat", ph:"dp", q:"Max loot from houses on a circle, no two adjacent hits.",
  o:["Linear robber twice — drop house 0, then drop house n−1","One pass of the plain linear DP over the whole circle as-is","Greedy: rob alternating houses, starting from the richer end","Interval DP over arcs of the circle, Burst Balloons style"], a:0,
  x:"The circle welds house 0 to n−1 — a linear pass happily robs both. Two runs, each excluding one end, break the weld; alternating greedy can't even solve the straight-line version." },
{ id:"q20", t:"pat", ph:"dp", q:"Minimum path sum top-left to bottom-right, moving only right or down.",
  o:["Grid DP: each cell adds min(top, left)","Dijkstra with a heap over the whole weighted grid","Plain BFS — first arrival at a cell wins","Backtrack all paths, keeping the best"], a:0,
  x:"Right/down only ⇒ a DAG, so a two-term recurrence settles each cell. Dijkstra also lands the answer but is a fire truck for a candle; BFS's first arrival isn't the cheapest when cells carry weights." },
{ id:"q25", t:"bug", ph:"dp", q:"A memoised fib that is still exponential.",
  code:"def fib(n):\n    if n < 2: return n\n    if n in memo: return memo[n]\n    return fib(n-1) + fib(n-2)",
  o:["memo is read but never written — store before returning","The `n in memo` lookup must come before the n < 2 base case","memo must be threaded through as a parameter, not shared globally","The base case n < 2 is off by one and should be n <= 2"], a:0,
  x:"A cache that's read but never written is a rumour — memo[n] = the sum, then return it. Lookup order is cosmetic (bases are O(1)), and a shared memo is exactly what you want here." },
{ id:"q26", t:"bug", ph:"dp", q:"House Robber II submitted as the plain linear DP over the whole array. Why wrong?",
  o:["It can rob houses 0 and n−1 together — adjacent once the row closes into a circle","The recurrence must reach back three houses, not two, on a circle","Nothing — dp[i−1] vs dp[i−2] already keeps neighbours apart","It should try every rotation of the array and keep the best"], a:0,
  x:"The circle welds the ends; the linear pass happily robs both. Run the line twice — without house 0, without house n−1. Rotations don't fix it: every rotation still lets its own two ends be robbed together." },
{ id:"q27", t:"bug", ph:"dp", q:"This Coin Change returns 0 for every amount. Why?",
  code:"dp = [0] * (amount + 1)\nfor a in range(1, amount + 1):\n    for c in coins:\n        if c <= a:\n            dp[a] = min(dp[a], dp[a-c] + 1)",
  o:["Seeded all 0, min() can never rise — init inf, keep dp[0] = 0","The loops are inverted: coins must run outer, amounts inner","The DP itself is fine — only a final inf → −1 translation is missing","The relaxation wants max, not min, to climb away from the zero floor"], a:0,
  x:"min(0, anything) is 0 — the seed must be inf so 'unknown' can't masquerade as 'free'. dp[0] = 0 stays: zero coins make zero. Loop order changes nothing for min-coins (that trap belongs to counting), and there's no inf here to translate." },
{ id:"q33", t:"bug", ph:"dp", q:"Plain recursive Climbing Stairs times out at n = 45. Root cause?",
  o:["The same subproblems recompute exponentially — one cache ends it","Recursion depth 45 overruns Python's stack, so the runtime thrashes","Python call overhead — the same tree, iterative, would pass","Two branches per call, and f(n−2) should be computed first"], a:0,
  x:"fib-shaped recursion revisits each n astronomically often — memoise or go bottom-up. Depth is a red herring: the stack holds 45 frames at worst; it's the 2^45-leaf tree that kills you, in any language, in any branch order." },
{ id:"q34", t:"bug", ph:"dp", q:"This LIS returns weirdly small answers.",
  code:"for i in range(n):\n    dp[i] = 1\n    for j in range(i):\n        if nums[j] < nums[i]:\n            dp[i] = dp[j] + 1",
  o:["Overwrites instead of keeping the best — needs max(dp[i], dp[j] + 1)","nums[j] < nums[i] rejects equal neighbours — strictness should be <=","The inner loop never looks past i, yet later elements can extend too","dp[i] = 1 re-runs inside the outer loop instead of once up front"], a:0,
  x:"The last qualifying j wins instead of the best — max() or nothing. dp[i] means 'best ending at i', so looking past i is backwards, and resetting dp[i] = 1 per i is exactly right. Strictness matches 'strictly increasing'." },
{ id:"q37", t:"line", ph:"dp", q:"First line inside a memoised solver.",
  code:"def solve(state):\n    ____",
  o:["if state in memo: return memo[state]","if memo[state]: return memo[state]","return memo.get(state, solve(state))","memo[state] = memo.get(state, 0) + 1"], a:0,
  x:"Check, then return, before any work — that's the whole speed-up. memo.get(state, solve(state)) evaluates the fallback eagerly (Python isn't lazy), so it recurses anyway; bare memo[state] raises KeyError on every miss." },
{ id:"q39", t:"line", ph:"dp", q:"Climbing Stairs, bottom-up.",
  code:"dp[i] = ____",
  o:["dp[i-1] + dp[i-2]","dp[i-1] + dp[i-2] + 1","dp[i-1] * dp[i-2]","max(dp[i-1], dp[i-2])"], a:0,
  x:"Routes are being counted, so the two ways in add. max() answers an optimisation question, and + 1 would tally the step itself rather than the paths through it." },
{ id:"q40", t:"line", ph:"dp", q:"Maximal Square, on a 1-cell.",
  code:"dp[r][c] = ____",
  o:["1 + min(dp[r-1][c], dp[r][c-1], dp[r-1][c-1])","1 + max(dp[r-1][c], dp[r][c-1], dp[r-1][c-1])","dp[r-1][c] + dp[r][c-1] - dp[r-1][c-1]","1 + min(dp[r-1][c], dp[r-1][c-1])"], a:0,
  x:"A square is only as big as its weakest supporting corner: min of all three, plus this cell. max() claims support the neighbours can't give; two neighbours aren't enough — the left one vetoes too; add-subtract belongs to prefix-sum tables." },
{ id:"q42", t:"line", ph:"dp", q:"Coin Change relaxation.",
  code:"dp[a] = min(dp[a], ____)",
  o:["dp[a - c] + 1","dp[a - 1] + c","dp[a - c] + c","dp[c] + dp[a - c]"], a:0,
  x:"One more coin — its count, not its value — on top of the best for the remainder. dp[a - c] + c charges pennies instead of coins; dp[c] + dp[a - c] forgets dp already owns any split of a." },
{ id:"q45", t:"line", ph:"dp", q:"Word Break inner update, for a split point j < i.",
  code:"dp[i] = dp[i] or ____",
  o:["dp[j] and s[j:i] in words","dp[j] or s[j:i] in words","dp[i-1] and s[i-1] in words","dp[j] and s[j:] in words"], a:0,
  x:"A breakable prefix through j and one dictionary word bridging j→i. or lets an unbreakable prefix sneak through; s[j:] overshoots i; stepping one letter at a time only ever finds single-character words." },
{ id:"q49", t:"cx", ph:"dp", q:"The classic two-loop LIS?",
  o:["O(n²) time, O(n) space","O(n log n) time, O(n) space","O(n²) time, O(n²) space","O(n³) time, O(n) space"], a:0,
  x:"Two nested loops, one dp array. n log n is real but it's patience sorting — tails + binary search, a different algorithm. No table, no cube: each dp[i] is a single number." },
{ id:"q51", t:"cx", ph:"dp", q:"Memoised Fibonacci?",
  o:["O(n) time, O(n) space","O(2^n) time, O(n) space","O(n) time, O(1) space","O(n²) time, O(n) space"], a:0,
  x:"Each n solved once, stored once — linear both ways (the memo and the call stack). O(1) space is the two-variable loop, a different implementation; without the memo you'd earn the 2^n." },
{ id:"q52", t:"cx", ph:"dp", q:"Subset-sum DP, n items, target T?",
  o:["O(n·T) — pseudo-polynomial","O(n·T) — genuinely polynomial","O(2^n) — subsets stay exponential","O(n·log T) via binary search"], a:0,
  x:"n·T cells, O(1) each — but T is a value, so the bound is exponential in T's bit-length. That's what 'pseudo' means; calling it genuinely polynomial is the classic trap. The 2^n wall falls precisely because sums repeat." },
{ id:"q81", t:"pat", ph:"dp", q:"One buy, then one later sell — max stock profit in a single pass.",
  o:["Track the running min; best = today − that min","Kadane on the raw prices, restarting after every dip","Buy at the global minimum, sell at the global maximum","Two pointers from both ends, stepping the cheaper inward"], a:0,
  x:"Best Time to Buy and Sell Stock. Each day's sale pairs with the cheapest earlier buy — a running min is the whole DP. Global min/max ignores order (the max can come first); Kadane belongs on the day-to-day deltas, not the prices." },
{ id:"q82", t:"pat", ph:"dp", q:"Max subarray sum in one pass, O(1) extra space.",
  o:["Kadane: extend while the running sum helps, restart at zero","Divide and conquer with a best-crossing-the-middle merge","Sum each all-positive stretch and keep the largest","Sort the prefix sums and take the widest gap"], a:0,
  x:"Kadane — a negative running sum only drags down whatever follows. Divide and conquer also solves it, but in O(n log n) with a recursion stack; all-positive stretches miss spans over a dip ([2,−1,3] ⇒ 4); sorted prefix sums forget order." },
{ id:"q83", t:"pat", ph:"dp", q:"Assign + or − to every number so the total hits target T. Count the ways.",
  o:["Subset-sum count for (total + T) / 2 — 0/1 knapsack","Layered BFS carrying a set of the reachable running sums","Greedy: give + to big numbers until T is passed","Unbounded knapsack on T, one state per amount"], a:0,
  x:"Target Sum. Fix the plus-pile P: P − (total − P) = T ⇒ P = (total + T)/2 — count subsets hitting it. A plain reachable-set answers 'possible?', not 'how many ways'; unbounded transitions reuse numbers that are strictly use-once." },
{ id:"q84", t:"pat", ph:"dp", q:"Longest palindromic substring, without paying O(n²) memory.",
  o:["Expand around all 2n−1 centres — letters and gaps","Interval DP: s[i..j] good if ends match and inside is","Reverse s, take the longest common substring with it","A window that grows while it reads the same both ways"], a:0,
  x:"Every palindrome has a centre — letter or gap — so expand from each of the 2n−1: O(n²) time, O(1) space. The DP table is correct but spends the memory the ask forbids; reversed-LCS breaks on abacdgfdcaba — a reversed copy needn't be a palindrome." },
{ id:"q85", t:"bug", ph:"dp", q:"Decode Ways ('1'→A … '26'→Z) — but '106' comes back 2 instead of 1. The bug?",
  code:"dp = [1] + [0] * len(s)\nfor i in range(1, len(s) + 1):\n    dp[i] = dp[i-1]\n    if i > 1 and 10 <= int(s[i-2:i]) <= 26:\n        dp[i] += dp[i-2]",
  o:["dp[i] = dp[i-1] banks a lone digit even when s[i-1] is '0' — guard it","int(s[i-2:i]) turns '06' into 6, letting zero-led pairs count","dp[0] = 1 credits the empty string with a decoding","The pair window is off by one — it should read s[i-1:i+1]"], a:0,
  x:"A lone '0' decodes to nothing, so the single-digit take needs if s[i-1] != '0'. The pair check already rejects '06' — 10 <= 6 is false — and dp[0] = 1 is the legitimate one-way-to-decode-nothing base." },
{ id:"q86", t:"bug", ph:"dp", q:"A 1D 0/1 knapsack that returns more value than the items allow. Why?",
  code:"dp = [0] * (W + 1)\nfor w, v in items:\n    for c in range(w, W + 1):\n        dp[c] = max(dp[c], dp[c-w] + v)",
  o:["Ascending c reads dp[c-w] already boosted by this item — iterate c downward","The loops are inverted — capacity belongs on the outside for a rolling array","range(w, W + 1) never fills capacities below w, so answers are lost","max() lets stale totals survive — dp[c] should be assigned, not compared"], a:0,
  x:"Right-to-left is the 0/1 trick: dp[c-w] must still mean 'without this item'. Ascending order re-reads a cell this very item just improved — unbounded knapsack by accident. Capacities below w can't fit the item, so skipping them is correct." },
{ id:"q87", t:"bug", ph:"dp", q:"Coin Change 2 — count the combinations. This returns too many. The bug?",
  code:"dp = [0] * (amount + 1)\ndp[0] = 1\nfor a in range(1, amount + 1):\n    for c in coins:\n        if c <= a:\n            dp[a] += dp[a-c]",
  o:["Amounts outer counts 1+2 and 2+1 separately — put coins outer","dp[0] = 1 plants a phantom way that inflates every reachable amount","dp[a] += dp[a-c] compounds across coins — it needs min(), Coin Change I style","c <= a admits c == a, so each single-coin amount is double-counted"], a:0,
  x:"With amounts outer every ordering of a multiset is tallied — permutations. Coins outer commits to a coin order, building each combination exactly once. dp[0] = 1 is the honest base (one way to make nothing), and c == a is the legitimate single-coin way." },
{ id:"q88", t:"line", ph:"dp", q:"Edit distance, when the current characters differ.",
  code:"if a[i-1] == b[j-1]:\n    dp[i][j] = dp[i-1][j-1]\nelse:\n    dp[i][j] = ____",
  o:["1 + min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1])","min(dp[i-1][j] + 1, dp[i][j-1] + 1, dp[i-1][j-1] + 2)","1 + max(dp[i-1][j], dp[i][j-1], dp[i-1][j-1])","min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1])"], a:0,
  x:"One edit — insert, delete, or replace — on top of the best smaller alignment. Without the 1, edits are free and everything costs 0; pricing replace at 2 answers the delete+insert-only metric, not Levenshtein." },
{ id:"q89", t:"line", ph:"dp", q:"Longest Common Subsequence — the matching-ends case.",
  code:"if a[i-1] == b[j-1]:\n    dp[i][j] = ____\nelse:\n    dp[i][j] = max(dp[i-1][j], dp[i][j-1])",
  o:["dp[i-1][j-1] + 1","dp[i-1][j] + 1","dp[i][j-1] + 1","dp[i-1][j-1]"], a:0,
  x:"A match extends the best alignment that is one shorter in both strings. dp[i-1][j] + 1 lets a character of b pair twice — try a = \"aa\", b = \"a\"; dropping the + 1 never banks the match at all." },
{ id:"q90", t:"line", ph:"dp", q:"Min Cost Climbing Stairs — dp[i] is the cheapest way to stand on step i; the top is step n.",
  code:"dp = [0] * (n + 1)\nfor i in range(2, n + 1):\n    dp[i] = ____\nreturn dp[n]",
  o:["min(dp[i-1] + cost[i-1], dp[i-2] + cost[i-2])","min(dp[i-1] + cost[i], dp[i-2] + cost[i-1])","min(dp[i-1], dp[i-2]) + cost[i-1]","dp[i-1] + min(cost[i-1], cost[i-2])"], a:0,
  x:"You pay a stair as you push off it, so each branch carries its own launch cost. A shared cost term charges the wrong stair half the time — and cost[i] doesn't even exist when i = n; the top sits past the last stair." },
{ id:"q91", t:"cx", ph:"dp", q:"Edit distance with the table rolled to two rows, strings sized n and m?",
  o:["O(n·m) time, O(min(n, m)) space","O(n·m) time, O(max(n, m)) space","O(n + m) time, O(min(n, m)) space","O(n·m) time, O(n·m) space"], a:0,
  x:"Every cell is still computed — time stays O(n·m) — but only the previous row is read, so keep two, and lay the shorter string across the columns for O(min). Roll without swapping and you're stuck with O(max); rolling never buys time." },
{ id:"q92", t:"cx", ph:"dp", q:"Unique Paths on an m×n grid, filled with a single rolling row?",
  o:["O(m·n) time, O(n) space","O(m·n) time, O(m·n) space","O(m + n) time, O(1) space","O(m·n) time, O(m + n) space"], a:0,
  x:"One row of width n, refreshed m times — every cell computed once. O(m + n) is real but belongs to the closed form C(m+n−2, m−1), not to any table fill; and nothing needs a row and a column both." },

{ id:"q14", t:"pat", ph:"ds", q:"Return the k most frequent elements of a big array.",
  o:["Count into a hash map, then a size-k min-heap","Sort the raw array and scan runs for winners","A size-k max-heap, popping the root on overflow","Quickselect the raw values around the kth largest"], a:0,
  x:"Count first; a size-k min-heap evicts its smallest, so the k biggest counts survive — n log k. A max-heap of size k pops the root, i.e. your best candidate. Bucket-by-count is the O(n) flex." },
{ id:"q15", t:"pat", ph:"ds", q:"Report the kth largest value after every insert of a stream — cheap per insert.",
  o:["A size-k min-heap — its root is the kth largest","A size-k max-heap, reading the kth from its root","A sorted list, bisecting each insert into place","Quickselect over everything after each insert"], a:0,
  x:"Small values fall out; the gatekeeper at the root is exactly kth — O(log k) per insert. A max-heap's root is the 1st largest, not the kth, and trimming it to size k evicts the biggest values first." },
{ id:"q28", t:"bug", ph:"ds", q:"This trie claims 'app' is a word right after inserting only 'apple'.",
  code:"def search(word):\n    node = root\n    for ch in word:\n        if ch not in node.kids:\n            return False\n        node = node.kids[ch]\n    return True",
  o:["The walk only proves a prefix — it should return node.end","node = node.kids[ch] advances a step past the last char","return True should trigger inside the loop, on the last char","The not-in test rejects children that are present"], a:0,
  x:"Reaching a node proves a prefix exists, nothing more — words carry a flag, so return node.end. The descent itself is sound: the not-in guard runs before every step down, and the loop stops exactly at the last char." },
{ id:"q50", t:"cx", ph:"ds", q:"Building a heap from n unsorted items (heapify)?",
  o:["O(n)","O(n log n)","O(n log log n)","O(n²)"], a:0,
  x:"The classic gotcha: sift down from the middle, and most nodes sit a hop from the leaves — the per-level sums telescope to linear. n log n is the price of n pushes; heapify wins by never lifting the leaves." },
{ id:"q53", t:"pat", ph:"ds", q:"Merge k sorted lists into one sorted stream, efficiently.",
  o:["A min-heap over the current head of each list","Concatenate everything, then one big sort","Merge the lists pairwise, left to right down the row","k pointers, rescanning every list for each output"], a:0,
  x:"Pop the global smallest, push its successor — O(N log k). Pairwise merging works only fold-style (divide and conquer); a left-to-right chain re-walks the growing list ⇒ O(Nk). Concatenate-and-sort forfeits the sortedness you were handed." },
{ id:"q54", t:"bug", ph:"ds", q:"Trie insert — but search never finds complete words.",
  code:"def insert(w):\n    node = root\n    for ch in w:\n        node = node.kids.setdefault(ch, Node())",
  o:["Nothing marks the terminal node after the loop","setdefault reuses old children where fresh nodes are needed","node must be rebound to root between characters","Node() is constructed even on hits, corrupting existing kids"], a:0,
  x:"The path gets laid but the flag never gets planted: node.end = True after the loop, or search can't tell 'apple' from 'app'. setdefault is the right call — it keeps shared prefixes; the spare Node() it builds on hits is wasted, not harmful." },
{ id:"q55", t:"cx", ph:"ds", q:"Pushing then popping n items through a binary heap?",
  o:["O(n log n)","O(n)","O(n²)","O(n √n)"], a:0,
  x:"2n operations, each O(log n) — heapsort in a trench coat. O(n) heapify only covers the building half; the n pops still pay log each, and a comparison sort can't beat n log n anyway." },
{ id:"q93", t:"pat", ph:"ds", q:"Stream in numbers; after each one, report the median so far.",
  o:["Max-heap for the low half, min-heap for the high half","A min-heap holding the low half, a max-heap the high half","One min-heap of everything, popped to the middle per query","A sorted list, bisect each arrival into its slot"], a:0,
  x:"Find Median from Data Stream: the roots meet in the middle — the low half's biggest and the high half's smallest are the median's ingredients, O(log n) per insert. Swap the heaps and the roots face outward, showing the global min and max instead." },
{ id:"q94", t:"pat", ph:"ds", q:"Maximum of every length-k window over an array, in one pass.",
  o:["A deque of indices, kept decreasing front to back","A max-heap rebuilt from scratch on every slide","A running max, patched whenever it exits the window","A monotonic increasing stack, popped as the window moves"], a:0,
  x:"Sliding Window Maximum: kill smaller values from the back — they can no longer win — and drop the front once it leaves the window, so the front is always the max, amortised O(n). A lone running max breaks the moment the max itself slides out: repairing it means rescanning, O(nk)." },
{ id:"q95", t:"pat", ph:"ds", q:"A cache with O(1) get and put that evicts the least recently touched key.",
  o:["Hash map into the nodes of a doubly-linked list","Hash map plus a min-heap keyed on last-use time","A queue of keys re-enqueued on each touch, plus a map","A singly-linked list with a map into its nodes"], a:0,
  x:"LRU Cache: the map buys O(1) lookup, the doubly-linked list buys O(1) unlink-and-move-to-front — you need prev to splice a node out, which is where singly-linked dies. A heap makes every touch O(log n), and a queue can't erase a key's old position." },
{ id:"q96", t:"pat", ph:"ds", q:"One structure must answer both search(word) and startsWith(prefix).",
  o:["One trie — a node walk answers both queries","A hash set of the words, probed per query","A hash set of every prefix of every word","A trie over the reversed words, walked from the ends"], a:0,
  x:"Implement Trie: startsWith asks 'did the walk survive', search additionally asks node.end — one structure, both in O(L). A set of words can't see prefixes without a full scan; a set of all prefixes says 'app' is a word after only 'apple' went in. Reversed tries answer endsWith." },
{ id:"q97", t:"bug", ph:"ds", q:"Daily Temperatures with a monotonic stack — it crashes or answers garbage.",
  code:"res = [0] * len(temps)\nst = []  # indices\nfor i, t in enumerate(temps):\n    while st and temps[st[-1]] < t:\n        j = st.pop()\n        res[j] = i - j\n    st.append(t)",
  o:["st.append(t) pushes the temperature where an index belongs","The < should be <=, so ties count as a warmer day","res[j] = i - j stores a day gap; temps[i] belongs there","j is popped before res[j] is written — order swapped"], a:0,
  x:"The stack must hold indices: res[j] = i - j needs positions, and temps[st[-1]] treats whatever is stored as one — push t and you index temps by a temperature. The strict < is right; an equally warm day isn't warmer." },
{ id:"q98", t:"bug", ph:"ds", q:"Valid Parentheses — rejects ')(' correctly, yet accepts '((('. Where?",
  code:"for ch in s:\n    if ch in pairs:            # closer -> opener\n        if not st or st.pop() != pairs[ch]:\n            return False\n    else:\n        st.append(ch)\nreturn True",
  o:["The last line waves leftovers through — return not st","st.pop() can fire on an empty stack and raise IndexError","pairs is inverted — it should map openers to closers","ch in pairs matches openers too — 'in' scans values, not keys"], a:0,
  x:"Openers with nothing to close them survive to the end — a non-empty stack is a failure, so finish with return not st. The pop is safe ('not st or …' short-circuits), and 'in' on a dict tests keys only, so closers route correctly." },
{ id:"q99", t:"bug", ph:"ds", q:"A min-stack with a second stack of minimums — getMin lies after popping a duplicate min.",
  code:"def push(x):\n    st.append(x)\n    if not mins or x < mins[-1]:\n        mins.append(x)\n\ndef pop():\n    x = st.pop()\n    if x == mins[-1]:\n        mins.pop()\n    return x",
  o:["push's strict < skips a tied minimum, so one pop strands the other","pop's == should be >= so larger values also shed mins","mins.append must come before st.append to stay in step","pop should take one entry off mins to keep the heights equal"], a:0,
  x:"Push 2, push 2: with <, mins holds a single 2 — the first pop evicts it and the surviving 2 has no minimum on record. Push ties with <=. Popping mins on every pop is the other design (pair a min with every element); with a sparse mins it deletes history that's still live." },
{ id:"q100", t:"line", ph:"ds", q:"A binary heap flattened into a 0-indexed array.",
  code:"parent = (i - 1) // 2\nleft = ____\nright = left + 1",
  o:["2 * i + 1","2 * i","2 * i + 2","(i + 1) // 2"], a:0,
  x:"Zero-indexed: children at 2i + 1 and 2i + 2, parent at (i - 1) // 2. 2i is the 1-indexed formula — and at i = 0 it names the root its own child, so the sift never moves." },
{ id:"q101", t:"line", ph:"ds", q:"heapq only speaks min-heap. Finish the max-heap pop.",
  code:"heappush(h, -x)   # arrivals go in negated\ntop = ____",
  o:["-heappop(h)","heappop(h)","-h[0]","h.pop()"], a:0,
  x:"Negate going in, negate coming out — the smallest negative is the largest original. Bare heappop(h) hands back a negated value, -h[0] only peeks, and h.pop() takes the array's last slot, which is nobody in particular." },
{ id:"q102", t:"line", ph:"ds", q:"Trie startsWith(p) — the walk survived. Last line?",
  code:"node = root\nfor ch in p:\n    if ch not in node.kids:\n        return False\n    node = node.kids[ch]\n____",
  o:["return True","return node.end","return bool(node.kids)","return not node.kids"], a:0,
  x:"Surviving the walk is the whole test — the prefix exists. node.end is search's ending, and bool(node.kids) wrongly rejects a prefix that is itself a whole word with nothing after it." },
{ id:"q103", t:"cx", ph:"ds", q:"Trie search for a word of length L against n stored words?",
  o:["O(L)","O(L log n)","O(log n)","O(L + n)"], a:0,
  x:"One node per character, each child lookup O(1) — the dictionary's size never enters. That independence from n is the sales pitch over a sorted array, whose prefix binary search pays O(L log n)." },
{ id:"q104", t:"cx", ph:"ds", q:"Sliding Window Maximum with a deque — n elements, window k?",
  o:["O(n)","O(n log k)","O(n k)","O(n log n)"], a:0,
  x:"Each index enters the deque once and leaves once — two O(1) touches per element, whatever k is. The log k bill belongs to the heap version; n·k is the rescan-every-window baseline." },
];

export const DECKS: Deck[] = [
  { id: "mix", n: "Mix" }, { id: "graphs", n: "Graphs" }, { id: "bt", n: "Backtracking" },
  { id: "dp", n: "DP" }, { id: "ds", n: "Heaps & tries" }, { id: "redo", n: "Redemption" },
];

export const CATN: Record<QType, string> = { pat: "Pattern", bug: "Bug hunt", line: "Missing line", cx: "Complexity" };

export const Q_BY_ID: Record<string, Question> = {};
Q.forEach((q) => { Q_BY_ID[q.id] = q; });
